#include "main.h"

/**
 * mul - multiply two int
 * @x: The first number to be multiplied
 * @y: The second number to be multplied with first
 * Return: The result of multiplication between x parameter and y parameter
 */
int mul(int x, int y)
{
return (x * y);
}
